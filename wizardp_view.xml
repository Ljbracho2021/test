<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Definición del wizardp -->
    <record id="report_wizardp_view_form" model="ir.ui.view">
        <field name="name">report.wizardp.view.form</field>
        <field name="model">report.wizardp</field>
        <field name="arch" type="xml">
            <form string="Informe de Ventas">
                <sheet>
                   
                   
                   <footer>
                        <button name="action_search_salida" type="object" string="Generar Reporte" class="btn-primary" />
                        <button string="Cancelar" class="btn-secondary" special="cancel" />
                   </footer>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Definición de la acción -->
    <record id="report_wizardp_action" model="ir.actions.act_window">
        <field name="name">Parroquias</field>
        <field name="res_model">report.wizardp</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="report_wizardp_view_form" />
        <field name="target">new</field>
    </record>
	
	<!-- Definición del menú -->
    
        <menuitem
        id="report_menu_parroquia"
        name="Parroquias"
        action="report_wizardp_action"
        parent="menu_chequeo"
        sequence="7" />

</odoo>
